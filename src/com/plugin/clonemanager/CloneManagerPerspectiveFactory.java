package com.plugin.clonemanager;

import org.eclipse.ui.IPageLayout;
import org.eclipse.ui.IPerspectiveFactory;

public class CloneManagerPerspectiveFactory implements IPerspectiveFactory {

	//private static final String VIEW_ID = "com.plugin.clonemanager.views.TabularView";
	
	//private static final String BOTTOM = "bottom";
	
	@Override
	public void createInitialLayout(IPageLayout myLayout) {
		// TODO Auto-generated method stub
		//myLayout.addView(IPageLayout.ID_OUTLINE,IPageLayout.LEFT,0.30f,
			//		myLayout.getEditorArea());


		//IFolderLayout bot = myLayout.createFolder(BOTTOM,IPageLayout.BOTTOM,0.76f,
			//		myLayout.getEditorArea());
		//bot.addView(VIEW_ID);
	}

}
